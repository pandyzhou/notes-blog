# 本地开发：把这个文件复制为 .env 后填入真实值（.env 不会被提交）
# 线上部署：把这些变量填到 Cloudflare / EdgeOne 的环境变量里

# Notion 内部集成的 Token，ntn_ 开头
NOTION_TOKEN=ntn_在这里填你的Token

# 文章数据库的 ID，直接从 Notion 数据库页面的网址里复制那串 32 位字符
NOTION_DATABASE_ID=在这里填数据库ID

# （可选）站点完整网址，用于 sitemap 和 RSS
SITE_URL=https://notes-blog.pages.dev
